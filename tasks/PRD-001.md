# PRD-001 — Environment and Package Baseline

## Goal
Establish reproducible Python 3.11 + uv runtime and install module package.

## Inputs
- `pyproject.toml`
- `src/def_rgbtcc`

## Steps
1. Create environment: `uv venv .venv --python 3.11`.
2. Activate env.
3. Install torch/torchvision cu128.
4. Install module editable: `uv pip install -e .`.
5. Verify import path for `def_rgbtcc`.

## Outputs
- Active `.venv` with package installed.
- Import validation log.

## Acceptance Criteria
1. `python -c "import def_rgbtcc"` succeeds.
2. `python -c "from def_rgbtcc import build_reference_model"` succeeds.

## Status
- TODO
